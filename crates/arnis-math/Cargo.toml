[package]
name = "arnis-math"
version = "0.1.0"
edition = "2024"

[dependencies]
geo = "0.33.1"
itertools = "0.15.0"
rand = { version = "0.9.3", features = ["std", "std_rng"] }
rand_chacha = "0.9"
serde = { version = "1.0", features = ["derive"] }
